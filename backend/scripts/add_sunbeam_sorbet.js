
import cloudinary from "cloudinary";
import dotenv from "dotenv";
import mongoose from "mongoose";

dotenv.config();

// Configure Cloudinary
cloudinary.v2.config({
  cloud_name: process.env.CLOUDINARY_CLOUD_NAME,
  api_key: process.env.CLOUDINARY_API_KEY.trim(),
  api_secret: process.env.CLOUDINARY_API_SECRET.trim(),
});

const imagePath = "/Users/rishabhrathore/.gemini/antigravity/brain/0369984b-cab7-453c-b4f9-b350e6e8c3bb/media__1773228787991.jpg";

async function run() {
  try {
    console.log("Connecting to MongoDB...");
    await mongoose.connect(process.env.MONGO_URI);
    console.log("Connected to MongoDB");

    // Models
    const categorySchema = new mongoose.Schema({ name: String, slug: String });
    const Category = mongoose.models.Category || mongoose.model("Category", categorySchema);

    const productSchema = new mongoose.Schema({
      name: String,
      slug: String,
      description: String,
      images: [String],
      price: Number,
      categoryId: { type: mongoose.Schema.Types.ObjectId, ref: 'Category' },
      tags: [String],
      isActive: { type: Boolean, default: true },
      premiumWrapping: { type: Boolean, default: false }
    });
    const Product = mongoose.models.Product || mongoose.model("Product", productSchema);

    // 1. Upload to Cloudinary
    console.log("Uploading to Cloudinary...");
    const uploadRes = await cloudinary.v2.uploader.upload(imagePath, {
      folder: "products",
    });
    console.log("Uploaded:", uploadRes.secure_url);

    // 2. Get category
    const category = await Category.findOne({ slug: "flowers" });
    if (!category) throw new Error("Category 'flowers' not found");
    console.log("Found Category:", category.name, category._id);

    // 3. Create Product
    const productName = "Sunbeam & Sorbet Rose Symphony";
    let baseSlug = "sunbeam-sorbet-rose-symphony";
    let slug = baseSlug;
    const existing = await Product.findOne({ slug });
    if (existing) {
        slug += "-" + Math.floor(Math.random() * 1000);
    }
    
    const newProduct = new Product({
      name: productName,
      slug: slug,
      description: "A cheerful and vibrant mix of sun-kissed yellow roses and delicate bi-color white-and-pink roses. This bouquet is designed to radiate happiness and warmth, making it an ideal gift for brightening someone's day. Wrapped in layers of soft pink premium parchment and tied with a sunny yellow multi-layered bow.\n\n**This Product Consists of**:\n- **Premium Flowers**: 5-6 Vibrant yellow roses and 5-6 delicate bi-color white-and-pink roses.\n- **Accents**: Abundant fresh white baby's breath (Gypsophila).\n- **Packaging**: Premium pink textured parchment wrapping.\n- **Finishing**: Large yellow multi-layered ribbon bow.\n\n**Suitable for**: Birthdays, friendship celebrations, get-well wishes, or as a vibrant 'Just because' gift.",
      images: [uploadRes.secure_url],
      price: 950,
      categoryId: category._id,
      tags: ["yellow roses", "pink roses", "bi-color roses", "cheerful", "birthday", "friendship", "bouquet", "flowers"],
      isActive: true,
      premiumWrapping: true
    });

    const saved = await newProduct.save();
    console.log("Product saved successfully:", saved.name, "at Rs.", saved.price);

    process.exit(0);
  } catch (err) {
    console.error("Error:", err);
    process.exit(1);
  }
}

run();
